import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'geminiqtube.quangdlm.com',
  appName: 'GeminiQtube',
  webDir: 'dist',
  server: {
    url: 'https://geminiqtube.vercel.app',
  },
};

export default config;
